name: split_check

on:
  # 定时每天 4 次更新
  schedule:
    - cron: "0 0 * * *"    # 00:00 UTC
    - cron: "0 6 * * *"    # 06:00 UTC
    - cron: "0 12 * * *"   # 12:00 UTC
    - cron: "0 18 * * *"   # 18:00 UTC
  # 手动触发
  workflow_dispatch:
    inputs:
      part:
        description: "手动验证指定分片 0~15，不填则按轮替自动选择"
        required: false
        default: ""

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests dnspython

      - name: Print start time
        run: echo "⏱ Workflow 开始：$(date -u)"

      - name: Run split_and_check_16.py
        shell: bash
        run: |
          if [ "${{ github.event.inputs.part }}" != "" ]; then
              echo "手动触发，验证分片 ${{ github.event.inputs.part }}"
              python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
          else
              echo "自动轮替验证当前分片"
              python split_and_check_16.py | tee split_check.log

      - name: Commit and push changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/validated_*.txt tmp/failed_*.txt
          git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
          git push origin main

      - name: Print end time
        run: echo "⏱ Workflow 结束：$(date -u)"
