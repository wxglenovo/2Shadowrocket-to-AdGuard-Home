name: DNS Split Validation

on:
  workflow_dispatch:
    inputs:
      part:
        description: '分片编号 1~16（手动触发可选）'
        required: false
        default: ''
  schedule:
    # 每天更新 urls.txt 一次（0:00 UTC）
    - cron: "0 0 * * *"
    # 每 1.5 小时轮替自动验证分片（UTC 时间）
    - cron: "0 0,1,3,4,6,7,9,10,12,13,15,16,18,19,21,22 * * *"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.12

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run DNS validation
      run: |
        PART="${{ github.event.inputs.part }}"
        if [ -n "$PART" ]; then
          echo "🛠 手动触发，验证分片 $PART"
          python split_and_check_16.py --part "$PART" | tee split_check.log
        else
          echo "⏱ 自动轮替验证当前分片"
          python split_and_check_16.py | tee split_check.log

    - name: Commit & Push
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add dist/blocklist_valid.txt tmp/part_*.txt
        git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
        git push origin main || echo "⚠️ Push 失败，请检查权限"
