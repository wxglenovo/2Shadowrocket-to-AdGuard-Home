name: AdGuardHome Blocklist Validation

on:
  schedule:
    # 每天 16 次，UTC 时间，每 1.5 小时轮替
    - cron: "0 0,90,180,270,360,450,540,630,720,810,900,990,1080,1170,1260,1350 * * *"
  workflow_dispatch:
    inputs:
      part:
        description: '手动触发验证指定分片 (0-15)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  validate-blocklist:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run blocklist validation
        run: |
          PART="${{ github.event.inputs.part }}"
          echo "⏱ Workflow 开始：$(date -u)"
          
          if [ -n "$PART" ]; then
            echo "🛠 手动触发，验证分片 $PART"
            python split_and_check_16.py --part "$PART" | tee split_check.log
          else
            echo "⏱ 自动轮替验证当前分片"
            python split_and_check_16.py | tee split_check.log
          fi

          echo "⏱ Workflow 结束：$(date -u)"

      - name: Commit and push valid rules
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 仅在存在文件时提交
          if [ -f dist/blocklist_valid.txt ] || ls tmp/part_*.txt 1> /dev/null 2>&1; then
            git add dist/blocklist_valid.txt tmp/part_*.txt
            git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
            git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }} HEAD:main
          else
            echo "⚠️ 没有需要提交的文件，跳过 push"
