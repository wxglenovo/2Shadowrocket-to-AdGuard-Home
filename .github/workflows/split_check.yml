name: Split & DNS Check (Concurrent)

on:
  schedule:
    - cron: "0 0 * * *"
    - cron: "0 6 * * *"
    - cron: "0 12 * * *"
    - cron: "0 18 * * *"
    - cron: "*/22 * * * *"
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  concurrent-dns-check:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1
      DNS_WORKERS: 50
      DNS_BATCH_SIZE: 500
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install --upgrade requests dnspython tqdm

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Determine PART index
        id: detect
        run: |
          mkdir -p tmp
          LAST_PART_FILE="tmp/last_part.txt"
          PART_INPUT="${{ github.event.inputs.part }}"
          
          if [ -n "$PART_INPUT" ]; then
            PART="$PART_INPUT"
            echo "🛠 手动指定分片：$PART"
          else
            if [ -f "$LAST_PART_FILE" ]; then
              LAST_PART=$(cat "$LAST_PART_FILE")
              PART=$(( (LAST_PART % 16) + 1 ))
            else
              PART=1
            fi
            echo "⏱ 自动轮替分片：$PART"
          fi
          
          echo "$PART" > "$LAST_PART_FILE"
          echo "part=$PART" >> $GITHUB_OUTPUT

      - name: Ensure rules exist
        run: |
          MERGED_FILE="merged_rules.txt"
          FIRST_PART="tmp/part_01.txt"
          if [ ! -f "$MERGED_FILE" ] || [ ! -f "$FIRST_PART" ]; then
            echo "⚠ 缺少规则文件或分片 → 自动生成"
            python3 split_and_check_16.py
          else
            echo "✅ 规则文件和分片存在"

      - name: Ensure delete_counter.json exists
        run: |
          mkdir -p dist
          if [ ! -f dist/delete_counter.json ]; then
            echo "{}" > dist/delete_counter.json
            echo "✅ 创建 dist/delete_counter.json"
          else
            echo "✅ delete_counter.json 已存在"

      - name: Run concurrent DNS validation
        env:
          PART: ${{ steps.detect.outputs.part }}
          DNS_WORKERS: ${{ env.DNS_WORKERS }}
          DNS_BATCH_SIZE: ${{ env.DNS_BATCH_SIZE }}
        run: |
          mkdir -p logs
          echo "🚀 启动 $DNS_WORKERS 并发验证，每批 $DNS_BATCH_SIZE 条"
          python3 split_and_check_16.py --part "$PART" --concurrent | tee logs/split_check_part_${PART}.log

      - name: Commit & Push Validated Rules
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          STATS=$(grep "COMMIT_STATS" logs/split_check_part_${PART}.log | tail -n1 | sed 's/COMMIT_STATS: //')
          
          git add tmp/last_part.txt dist/validated_part_*.txt dist/delete_counter.json merged_rules.txt
          git commit -m "🤖 part $PART → $STATS" || echo "No changes to commit"
          
          git pull --rebase origin main || echo "⚠ Pull failed"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main || echo "⚠ Push failed"
