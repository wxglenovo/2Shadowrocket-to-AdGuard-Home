name: split_check

on:
  workflow_dispatch:
    inputs:
      part:
        description: '手动触发验证指定分片（0-15）'
        required: false
        default: ''
  schedule:
    # 每天更新 urls.txt 1 次（UTC 0:00）
    - cron: "0 0 * * *"
    # 每天 16 次轮替分片验证（UTC 时间）
    - cron: "0 0 * * *"
    - cron: "0 1 * * *"
    - cron: "0 3 * * *"
    - cron: "0 4 * * *"
    - cron: "0 6 * * *"
    - cron: "0 7 * * *"
    - cron: "0 9 * * *"
    - cron: "0 10 * * *"
    - cron: "0 12 * * *"
    - cron: "0 13 * * *"
    - cron: "0 15 * * *"
    - cron: "0 16 * * *"
    - cron: "0 18 * * *"
    - cron: "0 19 * * *"
    - cron: "0 21 * * *"
    - cron: "0 22 * * *"

jobs:
  split-check:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.12

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests dnspython

    - name: Run Split and Check
      run: |
        if [ "${{ github.event.inputs.part }}" != "" ]; then
          echo "🛠 手动触发，验证分片 ${{ github.event.inputs.part }}"
          python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
        else
          echo "⏱ 自动轮替验证当前分片"
          python split_and_check_16.py | tee split_check.log

    - name: Commit and push valid rules
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add dist/blocklist_valid.txt tmp/part_*.txt || echo "No files to add"
        git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
        git push origin main || echo "Push failed, maybe no changes"
