name: Split & DNS Check

on:
  schedule:
    - cron: "0 0 * * *"  # 每天 UTC 0:00 强制更新 urls.txt 并生成分片
    - cron: "0 0,1,3,4,6,7,9,10,12,13,15,16,18,19,21,22 * * *"  # 分片轮替验证
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install --upgrade requests dnspython

      - name: Update urls.txt and split (daily forced)
        shell: bash
        run: |
          CURRENT_HOUR=$(date -u +"%H")
          if [ "$CURRENT_HOUR" = "00" ]; then
              echo "🔄 每天强制更新 urls.txt 并生成分片"
              python split_and_check_16.py --force-update
          fi

      - name: Run split and DNS check
        shell: bash
        run: |
          LAST_PART_FILE=".last_part"
          PART_INPUT="${{ github.event.inputs.part }}"

          if [ -n "$PART_INPUT" ]; then
              PART="$PART_INPUT"
              echo "🛠 手动触发，验证分片 $PART"
          else
              if [ -f "$LAST_PART_FILE" ]; then
                  LAST_PART=$(cat "$LAST_PART_FILE")
                  PART=$(( (LAST_PART % 16) + 1 ))
              else
                  PART=1
              fi
              echo "⏱ 自动轮替验证分片 $PART"
          fi

          echo $PART > $LAST_PART_FILE

          # 检查分片文件是否存在，缺失则自动下载
          PART_FILE="tmp/part_${PART}.txt"
          if [ ! -f "$PART_FILE" ]; then
              echo "⚠ 分片缺失：$PART_FILE，自动重新下载规则源"
              python split_and_check_16.py --force-update
          fi

          python split_and_check_16.py --part "$PART" | tee logs/split_check_part_${PART}.log

      - name: Commit & Push Validated Rules
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if ls dist/validated_part_*.txt 1> /dev/null 2>&1; then
              git add dist/validated_part_*.txt tmp/part_*.txt logs/
              git commit -m "🤖 Auto update: validated part $PART" || echo "No changes to commit"
              git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
          else
              echo "⚠ 没有生成任何验证文件，跳过 commit & push"
          fi
