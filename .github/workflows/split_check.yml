name: split_check
name: Split and Check Blocklist

on:
  # 每天更新 urls.txt 一次
  schedule:
    - cron: "0 0 * * *"  # UTC 时间每天 00:00 更新 urls.txt
  workflow_dispatch:
    inputs:
      part:
        description: '手动触发验证指定分片（0-15）'
        required: false
        default: ''

jobs:
  update_and_validate:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.12

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests dnspython

    - name: Download urls.txt
      run: |
        mkdir -p tmp dist
        echo "📥 下载 urls.txt"
        curl -fsSL -o urls.txt https://raw.githubusercontent.com/你的用户名/你的仓库/main/urls.txt

    - name: Run Split and Check
      run: |
        if [ "${{ github.event.inputs.part }}" != "" ]; then
          echo "🛠 手动触发，验证分片 ${{ github.event.inputs.part }}"
          python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
        else
          echo "⏱ 自动轮替验证当前分片"
          python split_and_check_16.py | tee split_check.log
        fi

    - name: Commit and push results
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add dist/blocklist_valid.txt tmp/part_*.txt || echo "No changes"
        git commit -m "🤖 Auto update: valid blocklist" || echo "No changes to commit"
        git push origin main || echo "Push failed, maybe no changes"
