name: Split DNS Validation

on:
  schedule:
    # 每天 00:00 UTC 更新 urls.txt
    - cron: "0 0 * * *"
    # 每天轮替 16 个分片（示例 UTC 时间，每小时一次，循环覆盖）
    - cron: "0 0 * * *"
    - cron: "0 1 * * *"
    - cron: "0 2 * * *"
    - cron: "0 3 * * *"
    - cron: "0 4 * * *"
    - cron: "0 5 * * *"
    - cron: "0 6 * * *"
    - cron: "0 7 * * *"
    - cron: "0 8 * * *"
    - cron: "0 9 * * *"
    - cron: "0 10 * * *"
    - cron: "0 11 * * *"
    - cron: "0 12 * * *"
    - cron: "0 13 * * *"
    - cron: "0 14 * * *"
    - cron: "0 15 * * *"
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  update_and_validate:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Create directories
        run: |
          mkdir -p tmp dist

      - name: Download urls.txt
        run: |
          echo "📥 下载最新 urls.txt"
          curl -sSL -o urls.txt "https://raw.githubusercontent.com/wxglenovo/Shadowrocket-to-AdGuard-Home/main/urls.txt"
          echo "✅ 下载完成，条数：$(wc -l < urls.txt)"

      - name: Run validation
        run: |
          if [ "${{ github.event.inputs.part }}" != "" ]; then
            echo "🛠 手动触发，验证分片 ${{ github.event.inputs.part }}"
            python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
          else
            echo "⏱ 自动轮替验证当前分片"
            python split_and_check_16.py | tee split_check.log

      - name: Commit and push valid rules
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/part_*.txt
          git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
          git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }} HEAD:main
