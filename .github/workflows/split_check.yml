name: Split and Check Blocklist

on:
  schedule:
    # 每天 16 次轮替验证切片（每 1.5 小时一次，UTC 时间）
    - cron:  '0 0,1,3,4,6,7,9,10,12,13,15,16,18,19,21,22 * * *'
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证切片编号 (0-15)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run validation
        shell: bash
        env:
          PYTHONUNBUFFERED: 1
        run: |
          if [ "${{ github.event.inputs.part }}" != "" ]; then
            PART="${{ github.event.inputs.part }}"
            echo "🛠 手动触发，验证分片 $PART"
            python split_and_check_16.py --part $PART | tee split_check.log
          else
            echo "⏱ 自动轮替验证当前分片"
            python split_and_check_16.py | tee split_check.log

      - name: Commit & Push valid rules
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/part_*.txt
          git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
          git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git HEAD:main
