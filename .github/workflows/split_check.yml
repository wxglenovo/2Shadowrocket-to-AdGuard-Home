name: split_check

on:
  schedule:
    # 每天 16 次，每 25 分钟轮替
    - cron: "0 0 * * *"     # 00:00
    - cron: "25 0 * * *"    # 00:25
    - cron: "50 0 * * *"    # 00:50
    - cron: "15 1 * * *"    # 01:15
    - cron: "40 1 * * *"    # 01:40
    - cron: "5 2 * * *"     # 02:05
    - cron: "30 2 * * *"    # 02:30
    - cron: "55 2 * * *"    # 02:55
    - cron: "20 3 * * *"    # 03:20
    - cron: "45 3 * * *"    # 03:45
    - cron: "10 4 * * *"    # 04:10
    - cron: "35 4 * * *"    # 04:35
    - cron: "0 5 * * *"     # 05:00
    - cron: "25 5 * * *"    # 05:25
    - cron: "50 5 * * *"    # 05:50
    - cron: "15 6 * * *"    # 06:15

  workflow_dispatch:
    inputs:
      part:
        description: "手动验证指定分片 0~15，不填则自动轮替"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        run: pip install requests dnspython

      - name: Validate Blocklist
        run: |
          echo "⏱ Workflow 开始：$(date -u)"
          if [ "${{ github.event.inputs.part }}" != "" ]; then
              echo "手动触发，验证分片 ${{ github.event.inputs.part }}"
              python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
          else
              echo "自动轮替验证当前分片"
              python split_and_check_16.py | tee split_check.log
          fi
          echo "⏱ Workflow 结束：$(date -u)"

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt dist/part_*.txt || echo "No changes"
          git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
          git push origin main || echo "Push failed, maybe no changes"
