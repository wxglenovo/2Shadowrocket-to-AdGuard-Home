name: split_check

on:
  schedule:
    # 每天 4 次更新 urls.txt 并切分规则
    - cron: "0 0 * * *"
  workflow_dispatch:
    inputs:
      part:
        description: "手动验证分片 0~15"
        required: false
        default: ""

jobs:
  split-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests dnspython

      - name: Update urls.txt & Split rules
        if: github.event_name == 'schedule'
        run: python split_and_check_16.py --split

      - name: Validate part
        run: |
          if [ "${{ github.event.inputs.part }}" != "" ]; then
            echo "手动触发，验证分片 ${{ github.event.inputs.part }}"
            python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
          else
            echo "自动轮替验证当前分片"
            python split_and_check_16.py | tee split_check.log
        env:
          PYTHONUNBUFFERED: 1

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/part_*.txt || echo "No changes"
          git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
          git push origin main || echo "Push failed"
