name: Split & DNS Check

on:
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证分片（0~15），留空自动轮替'
        required: false
        default: ''
  schedule:
    - cron: "0 0 * * *"   # 每天 UTC 00:00 更新 urls.txt
    - cron: "0 0,1,3,4,6,7,9,10,12,13,15,16,18,19,21,22,23 * * *" # 每 1.5 小时轮替验证分片

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests dnspython

    - name: Download urls.txt
      run: |
        mkdir -p tmp dist
        curl -s -o urls.txt https://raw.githubusercontent.com/wxglenovo/AdGuardHome-Filter/main/dist/urls.txt

    - name: Run Blocklist Validation
      run: |
        if [ "${{ github.event.inputs.part }}" != "" ]; then
          echo "🛠 手动触发，验证分片 ${{ github.event.inputs.part }}"
          python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
        else
          echo "⏱ 自动轮替验证当前分片"
          python split_and_check_16.py | tee split_check.log

    - name: Commit Validated Rules
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add dist/blocklist_valid.txt tmp/part_*.txt
        git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
        git push origin main || echo "⚠️ Push failed, 检查仓库权限"
