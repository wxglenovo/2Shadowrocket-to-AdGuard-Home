name: Split & DNS Check

# -----------------------------
# 触发条件
# -----------------------------
on:
  schedule:  # 定时任务
    - cron: "0 0 * * *"   # 每天 00:00 UTC
    - cron: "0 6 * * *"   # 每天 06:00 UTC
    - cron: "0 12 * * *"  # 每天 12:00 UTC
    - cron: "0 18 * * *"  # 每天 18:00 UTC
    - cron: "*/22 * * * *" # 每 22 分钟一次
  workflow_dispatch:      # 手动触发
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

# -----------------------------
# Job 定义
# -----------------------------
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1  # 输出实时刷新

    steps:

      # -----------------------------
      # 1. 检出仓库
      # -----------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # -----------------------------
      # 2. 设置 Python
      # -----------------------------
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      # -----------------------------
      # 3. 安装依赖
      # -----------------------------
      - name: Install dependencies
        run: pip install --upgrade requests dnspython

      # -----------------------------
      # 4. 配置 Git
      # -----------------------------
      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      # -----------------------------
      # 5. 确定当前分片 (PART)
      # -----------------------------
      - name: Determine PART index
        id: detect
        run: |
          mkdir -p tmp
          LAST_PART_FILE="tmp/last_part.txt"
          PART_INPUT="${{ github.event.inputs.part }}"

          if [ -n "$PART_INPUT" ]; then
            PART="$PART_INPUT"
            echo "🛠 手动指定分片：$PART"
          else
            if [ -f "$LAST_PART_FILE" ]; then
              LAST_PART=$(cat "$LAST_PART_FILE")
              PART=$(( (LAST_PART % 16) + 1 ))
            else
              PART=1
            fi
            echo "⏱ 自动轮替分片：$PART"
          fi

          echo "$PART" > "$LAST_PART_FILE"
          echo "part=$PART" >> $GITHUB_OUTPUT

      # -----------------------------
      # 6. 打印 tmp 目录，检查分片是否存在
      # -----------------------------
      - name: Check tmp parts
        run: |
          echo "📂 当前 tmp 目录内容："
          ls -lh tmp || echo "⚠ tmp 目录不存在"
          MISSING=0
          for i in $(seq -w 01 16); do
            if [ ! -f "tmp/part_${i}.txt" ]; then
              echo "⚠ tmp/part_${i}.txt 缺失"
              MISSING=1
            fi
          done
          echo "missing=$MISSING" >> $GITHUB_OUTPUT

      # -----------------------------
      # 7. 强制下载规则源（每天四次定时执行）或缺失分片
      # -----------------------------
      - name: Force download rules if scheduled or missing
        run: |
          CURRENT_HOUR=$(date -u +"%H")
          MISSING=${{ steps.check_tmp_parts.outputs.missing }}
          if [[ "$CURRENT_HOUR" == "00" || "$CURRENT_HOUR" == "06" || "$CURRENT_HOUR" == "12" || "$CURRENT_HOUR" == "18" || "$MISSING" == "1" ]]; then
            echo "✅ 强制下载规则源并生成分片"
            python3 split_and_check_16.py --force-update
          else
            echo "⏩ 非下载时间且分片存在，不强制更新"
          fi

      # -----------------------------
      # 8. 对当前分片进行 DNS 验证
      # -----------------------------
      - name: Run DNS validation for current part
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          mkdir -p logs
          echo "⏱ 开始验证分片 $PART"
          python3 split_and_check_16.py --part "$PART" | tee logs/split_check_part_${PART}.log

      # -----------------------------
      # 9. 提交并推送验证后的规则
      # -----------------------------
      - name: Commit & Push Validated Rules
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          STATS=$(grep "COMMIT_STATS" logs/split_check_part_${PART}.log | tail -n1 | sed 's/COMMIT_STATS: //')
          git add tmp/last_part.txt dist/validated_part_*.txt dist/delete_counter.json merged_rules.txt
          git commit -m "🤖 part $PART → $STATS" || echo "No changes to commit"
          git pull --rebase origin main || echo "⚠ Pull failed, 已忽略"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main || echo "⚠ Push failed, 已忽略"
