name: Split & DNS Check

on:
  schedule:
    # 每天 UTC 0:00 强制更新 urls.txt 并生成所有分片
    - cron: "0 0 * * *"
    # 每 1.5 小时轮替一片（含 23）
    - cron: "0 0,1,3,4,6,7,9,10,12,13,15,16,18,19,21,22,23 * * *"
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install --upgrade requests dnspython

      - name: Daily force update at UTC 00:00
        run: |
          HOUR=$(date -u +"%H")
          if [ "$HOUR" = "00" ]; then
            echo "🔄 每日强制刷新规则与切片"
            python split_and_check_16.py --force-update
          else
            echo "⏭ 当前不是 00:00，跳过强制更新"
          fi

      - name: Decide which part to run
        id: decide
        run: |
          PART_INPUT="${{ github.event.inputs.part }}"
          LAST_FILE=".last_part"

          if [ -n "$PART_INPUT" ]; then
              PART="$PART_INPUT"
              echo "🛠 手动请求验证分片 $PART"
          else
              if [ -f "$LAST_FILE" ]; then
                  LAST=$(cat "$LAST_FILE")
                  PART=$(( (LAST % 16) + 1 ))
              else
                  PART=1
              fi
              echo "⏱ 自动轮替验证分片 $PART"
          fi

          echo $PART > $LAST_FILE
          echo "part=$PART" >> $GITHUB_OUTPUT

      - name: Run split + validate with 50 threads
        run: |
          python split_and_check_16.py --part "${{ steps.decide.outputs.part }}" | head -n 500
          echo "⚠ 日志已截断避免 GitHub 报错，完整日志写入 split_check.log"

      - name: Save full log
        run: |
          mv split_check.log split_check_${{ steps.decide.outputs.part }}.log
          echo "✅ 完整日志已保存 split_check_${{ steps.decide.outputs.part }}.log"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dns-logs
          path: split_check_*.log

      - name: Commit & Push Validated Rules
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if ls dist/validated_part_*.txt 1> /dev/null 2>&1; then
            git add dist/validated_part_*.txt tmp/part_*.txt .last_part || true
            git commit -m "🤖 Auto update part ${{ steps.decide.outputs.part }}" || echo "No changes"
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
          else
            echo "⚠ 没有生成任何验证文件"
