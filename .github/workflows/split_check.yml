name: Split & DNS Check

on:
  schedule:
    # 每天 UTC 00:00 强制下载规则源 + 切片
    - cron: "0 0 * * *"
    # 每天轮替 16 次分片验证，每 1.5 小时一片
    - cron: "0 0,1,3,4,6,7,9,10,12,13,15,16,18,19,21,22,23 * * *"
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade requests dnspython

      - name: Detect PART index
        id: detect
        shell: bash
        run: |
          LAST_PART_FILE=".last_part"
          PART_INPUT="${{ github.event.inputs.part }}"

          if [ -n "$PART_INPUT" ]; then
            PART="$PART_INPUT"
            echo "手动指定分片：$PART"
          else
            if [ -f "$LAST_PART_FILE" ]; then
              LAST_PART=$(cat "$LAST_PART_FILE")
              PART=$(( (LAST_PART % 16) + 1 ))
            else
              PART=1
            fi
            echo "自动轮替分片：$PART"
          fi

          echo $PART > $LAST_PART_FILE
          echo "part=$PART" >> $GITHUB_OUTPUT

      - name: Force download & split at 00:00
        shell: bash
        run: |
          CURRENT_HOUR=$(date -u +"%H")
          if [ "$CURRENT_HOUR" = "00" ]; then
            echo "✅ 每天 00:00 强制下载规则源并切片"
            python split_and_check_16.py --force-update
          else
            echo "⏩ 非 00:00，不强制下载"
          fi

      - name: Check part file, re-download if missing
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          PART_FILE="tmp/part_${PART}.txt"

          if [ ! -f "$PART_FILE" ]; then
            echo "⚠ 分片缺失：$PART_FILE"
            echo "🔄 自动重新下载规则源并切片"
            python split_and_check_16.py --force-update
          else
            echo "✅ 分片存在：$PART_FILE"
          fi

      - name: Run DNS validation
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          echo "⏱ 开始验证分片 $PART"
          python split_and_check_16.py --part "$PART" | tee split_check.log

      - name: Store log safely
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          mkdir -p logs
          if [ -f split_check.log ]; then
            mv split_check.log "logs/split_check_part_${PART}.log"
            echo "✅ 日志已保存 logs/split_check_part_${PART}.log"
          else
            echo "⚠ split_check.log 未生成，跳过"
          fi

      - name: Set git config
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit & Push Validated Rules
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          if ls dist/validated_part_*.txt 1> /dev/null 2>&1; then
            git add dist/validated_part_*.txt tmp/part_*.txt logs/
            git commit -m "🤖 Auto update: validated part $PART" || echo "No changes to commit"
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
          else
            echo "⚠ 没有验证结果文件，跳过 commit & push"
