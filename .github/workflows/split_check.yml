name: Split & DNS Check

on:
  schedule:
    - cron: '0 0 * * *'          # 每天 0 点强制更新
    - cron: '0 */1 * * *'        # 每小时一次，自动轮替验证 1 个分片
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade requests dnspython

      - name: Daily forced update
        shell: bash
        run: |
          CURRENT_HOUR=$(date -u +"%H")
          if [ "$CURRENT_HOUR" = "00" ]; then
            echo "🔄 每天 00:00 强制更新规则源"
            python split_and_check_16.py --force-update
          else
            echo "⌛ 当前不是强制更新时间"
          fi

      - name: Run split & DNS check
        shell: bash
        run: |
          LAST_PART_FILE=".last_part"
          PART_INPUT="${{ github.event.inputs.part }}"

          if [ -n "$PART_INPUT" ]; then
            PART="$PART_INPUT"
            echo "🛠 手动触发 → 分片 $PART"
          else
            if [ -f "$LAST_PART_FILE" ]; then
              LAST_PART=$(cat "$LAST_PART_FILE")
              PART=$(( (LAST_PART % 16) + 1 ))
            else
              PART=1
            fi
            echo "⏱ 自动轮换 → 分片 $PART"
          fi

          echo "$PART" > "$LAST_PART_FILE"
          python split_and_check_16.py --part "$PART" | tee split_check.log

      - name: Commit & Push
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if ls dist/validated_part_*.txt 1> /dev/null 2>&1; then
            git add dist/validated_part_*.txt tmp/part_*.txt .last_part
            git commit -m "🤖 Auto update validated rules" || echo "No changes"
            git push "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" HEAD:main
          else
            echo "⚠ 没有新的验证文件，跳过提交"
