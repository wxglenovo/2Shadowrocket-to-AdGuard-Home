name: Split & DNS Check

on:
  schedule:
    - cron: "0 0 * * *"
    - cron: "0 6 * * *"
    - cron: "0 12 * * *"
    - cron: "0 18 * * *"
    - cron: "*/22 * * * *"

  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install --upgrade requests dnspython

      # 自动轮替分片 / 手动指定分片
      - name: Determine PART index
        id: detect
        shell: bash
        run: |
          mkdir -p tmp

          LAST_PART_FILE="tmp/last_part.txt"

          git fetch origin main
          git checkout origin/main -- tmp/last_part.txt || echo "⚠ tmp/last_part.txt 不存在"

          PART_INPUT="${{ github.event.inputs.part }}"

          if [ -n "$PART_INPUT" ]; then
            PART="$PART_INPUT"
            echo "🛠 手动指定分片：$PART"
          else
            if [ -f "$LAST_PART_FILE" ]; then
              LAST_PART=$(cat "$LAST_PART_FILE")
              PART=$(( (LAST_PART % 16) + 1 ))
            else
              PART=1
            fi
            echo "⏱ 自动轮替分片：$PART"
          fi

          echo $PART > $LAST_PART_FILE
          echo "part=$PART" >> $GITHUB_OUTPUT

      # 定时强制下载
      - name: Force download rules at schedule times
        shell: bash
        run: |
          CURRENT_HOUR=$(date -u +"%H")
          if [[ "$CURRENT_HOUR" == "00" || "$CURRENT_HOUR" == "06" || "$CURRENT_HOUR" == "12" || "$CURRENT_HOUR" == "18" ]]; then
            echo "✅ 强制下载规则源并切片"
            python split_and_check_16.py --force-update
          else
            echo "⏩ 非下载时间，不强制更新"
          fi

      # 如果缺合并规则或分片 → 自动生成
      - name: Ensure rules and first part exist
        shell: bash
        run: |
          if [ ! -f "merged_rules.txt" ] || [ ! -f "tmp/part_01.txt" ]; then
            echo "⚠ 缺失规则或分片，自动重新生成"
            python split_and_check_16.py --force-update
          else
            echo "✅ 规则和分片存在"
          fi

      # 当前分片缺失也会自动生成
      - name: Ensure current part file exists
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          PART_FILE=$(printf "tmp/part_%02d.txt" "$PART")
          if [ ! -f "$PART_FILE" ]; then
            echo "⚠ 当前分片缺失：$PART_FILE → 自动重新生成"
            python split_and_check_16.py --force-update
          else
            echo "✅ 当前分片存在：$PART_FILE"
          fi

      # ✅ 改成正确三参数调用方式
      - name: Run DNS validation
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          mkdir -p logs

          PART_FILE=$(printf "tmp/part_%02d.txt" "$PART")
          VALIDATED_OUT=$(printf "dist/validated_part_%02d.txt" "$PART")
          LOG_FILE=$(printf "logs/validate_part_%02d.log" "$PART")

          echo "⏱ 开始验证分片 $PART"
          python validate.py "$PART_FILE" "$VALIDATED_OUT" "$LOG_FILE"

      # ✅ 提取统计数并加入 commit 信息
      - name: Commit & Push Results
        shell: bash
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 查找最后一行统计
          STATS_LINE=$(grep -o "COMMIT_STATS: .*" logs/validate_part_${PART}.log | tail -1)

          git add tmp/last_part.txt dist/validated_part_*.txt tmp/part_*.txt logs/ merged_rules.txt
          git commit -m "🤖 Auto update Part $PART ($STATS_LINE)" || echo "No changes to commit"

          git pull --rebase origin main || echo "⚠ pull 失败，无需 rebase"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
