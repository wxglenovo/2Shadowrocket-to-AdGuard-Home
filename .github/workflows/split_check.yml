name: split_check

on:
  schedule:
    # 每天 4 次自动触发：00:00、06:00、12:00、18:00 UTC
    - cron:  "0 0 * * *"
    - cron:  "0 6 * * *"
    - cron:  "0 12 * * *"
    - cron:  "0 18 * * *"
  workflow_dispatch:
    inputs:
      part:
        description: "手动指定分片索引（0~15）"
        required: false
        default: ""

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests dnspython

      - name: Run Blocklist Validation
        env:
          PYTHONUNBUFFERED: 1  # 让 Python 输出实时刷新
        run: |
          echo "⏱ Workflow 开始：$(date -u)"

          # 判断手动触发是否指定分片
          if [ -n "${{ github.event.inputs.part }}" ]; then
              echo "⚡ 手动触发分片验证：part=${{ github.event.inputs.part }}"
              python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
          else
              echo "⚡ 自动轮替分片验证"
              python split_and_check_16.py | tee split_check.log
          fi

          echo "⏱ Workflow 结束：$(date -u)"

      - name: Commit and Push Validated Rules
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/validated_*.txt tmp/failed_*.txt || echo "无新增文件"
          git commit -m "🤖 Auto update: valid blocklist" || echo "无更改可提交"
          git push origin main || echo "推送失败或无更改"
