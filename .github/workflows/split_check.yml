name: Split & DNS Check

on:
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证分片编号（0-15），不填则自动轮替'
        required: false
        default: ''
  schedule:
    # 每天 UTC 0:00 更新 urls.txt
    - cron: "0 0 * * *"
    # 每 1.5 小时轮替验证分片（共 16 份）
    - cron: "0 0 * * *"
    - cron: "30 1 * * *"
    - cron: "0 3 * * *"
    - cron: "30 4 * * *"
    - cron: "0 6 * * *"
    - cron: "30 7 * * *"
    - cron: "0 9 * * *"
    - cron: "30 10 * * *"
    - cron: "0 12 * * *"
    - cron: "30 13 * * *"
    - cron: "0 15 * * *"
    - cron: "30 16 * * *"
    - cron: "0 18 * * *"
    - cron: "30 19 * * *"
    - cron: "0 21 * * *"
    - cron: "30 22 * * *"

permissions:
  contents: write

jobs:
  split-check:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests dnspython

      - name: Run split & DNS check
        shell: bash
        run: |
          echo "⏱ Workflow 开始：$(date -u)"

          if [ "${{ github.event.inputs.part }}" != "" ]; then
            echo "🛠 手动触发，验证分片 ${{ github.event.inputs.part }}"
            python split_and_check_16.py --part ${{ github.event.inputs.part }} | tee split_check.log
          else
            echo "⏱ 自动轮替验证当前分片"
            python split_and_check_16.py | tee split_check.log
          fi

          echo "⏱ Workflow 结束：$(date -u)"

      - name: Commit & push results
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          force: false
          commit_message: "🤖 Auto update: valid blocklist"
          directory: .
          # 只添加存在的文件
          file_pattern: |
            dist/blocklist_valid.txt
            tmp/part_*.txt
