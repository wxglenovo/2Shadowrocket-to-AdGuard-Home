name: Split & DNS Check

# -----------------------------
# 触发条件
# -----------------------------
on:
  schedule:
    - cron: "0 0 * * *"    # 每天 00:00 UTC
    - cron: "0 6 * * *"    # 每天 06:00 UTC
    - cron: "0 12 * * *"   # 每天 12:00 UTC
    - cron: "0 18 * * *"   # 每天 18:00 UTC
    - cron: "*/22 * * * *" # 每 22 分钟一次
  workflow_dispatch:
    inputs:
      part:
        description: '手动验证指定分片 1~16'
        required: false
        default: ''

permissions:
  contents: write  # 允许推送代码到仓库

# -----------------------------
# Job 定义
# -----------------------------
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: 1  # 输出实时刷新

    steps:

      # -----------------------------
      # 1. 检出仓库
      # -----------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史

      # -----------------------------
      # 2. 设置 Python 环境
      # -----------------------------
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      # -----------------------------
      # 3. 安装依赖
      # -----------------------------
      - name: Install dependencies
        run: pip install --upgrade requests dnspython

      # -----------------------------
      # 4. 配置 Git
      # -----------------------------
      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      # -----------------------------
      # 5. 确定当前分片 (PART)
      # -----------------------------
      - name: Determine PART index
        id: detect
        run: |
          mkdir -p tmp
          LAST_PART_FILE="tmp/last_part.txt"
          PART_INPUT="${{ github.event.inputs.part }}"

          if [ -n "$PART_INPUT" ]; then
            # 手动指定分片
            PART="$PART_INPUT"
            echo "🛠 手动指定分片：$PART"
          else
            # 自动轮替分片
            if [ -f "$LAST_PART_FILE" ]; then
              LAST_PART=$(cat "$LAST_PART_FILE")
              PART=$(( (LAST_PART % 16) + 1 ))
            else
              PART=1
            fi
            echo "⏱ 自动轮替分片：$PART"
          fi

          echo "$PART" > "$LAST_PART_FILE"
          echo "part=$PART" >> $GITHUB_OUTPUT

      # -----------------------------
      # 6. 定时下载规则源（每天 00/06/12/18）覆盖 tmp/part_**.txt
      # -----------------------------
      - name: Force download rules at schedule times
        run: |
          mkdir -p tmp
          CURRENT_HOUR=$(date -u +"%H")
          if [[ "$CURRENT_HOUR" == "00" || "$CURRENT_HOUR" == "06" || "$CURRENT_HOUR" == "12" || "$CURRENT_HOUR" == "18" ]]; then
            echo "✅ 定时下载规则源并生成所有分片"
            python3 split_and_check_16.py --force
          else
            # ✅ 非下载时间，如果 tmp/part_**.txt 存在则不下载
            if ls tmp/part_*.txt 1> /dev/null 2>&1; then
              echo "⏩ tmp 目录已有分片，不下载"
            else
              echo "⚠ tmp 目录无分片，首次拉取规则源"
              python3 split_and_check_16.py --force
            fi
          fi

      # -----------------------------
      # 7. 确保规则文件及首个分片存在
      # -----------------------------
      - name: Ensure rules and first part exist
        run: |
          MERGED_FILE="merged_rules.txt"
          FIRST_PART="tmp/part_01.txt"
          if [ ! -f "$MERGED_FILE" ] || [ ! -f "$FIRST_PART" ]; then
            echo "⚠ 缺少规则文件或分片 → 重新拉取"
            python3 split_and_check_16.py --force
          else
            echo "✅ 规则文件和分片存在"
          fi

      # -----------------------------
      # 8. 确保 delete_counter.json 存在
      # -----------------------------
      - name: Ensure delete_counter.json exists
        run: |
          mkdir -p dist
          if [ ! -f dist/delete_counter.json ]; then
            echo "{}" > dist/delete_counter.json
            echo "✅ 创建 dist/delete_counter.json"
          else
            echo "✅ delete_counter.json 已存在"
          fi

      # -----------------------------
      # 9. 对当前分片进行 DNS 验证
      # -----------------------------
      - name: Run DNS validation for current part
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          mkdir -p logs
          echo "⏱ 开始验证分片 $PART"
          python3 split_and_check_16.py --part "$PART" | tee logs/split_check_part_${PART}.log

      # -----------------------------
      # 10. 提交并推送验证后的规则
      # -----------------------------
      - name: Commit & Push Validated Rules
        env:
          PART: ${{ steps.detect.outputs.part }}
        run: |
          STATS=$(grep "COMMIT_STATS" logs/split_check_part_${PART}.log | tail -n1 | sed 's/COMMIT_STATS: //')
          git add tmp/last_part.txt dist/validated_part_*.txt dist/delete_counter.json merged_rules.txt
          git commit -m "🤖 part $PART → $STATS" || echo "No changes to commit"

          # ✅ 修改：使用 --autostash 避免 pull 报错
          git pull --rebase --autostash origin main || echo "⚠ Pull failed"

          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main || echo "⚠ Push failed"
