name: Update Blocklist 4x Daily

on:
  schedule:
    # 每天 UTC 0:00 / 6:00 / 12:00 / 18:00 执行
    - cron: "0 0 * * *"
    - cron: "0 6 * * *"
    - cron: "0 12 * * *"
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  update-blocklist:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 检出仓库
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ 设置 Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      # 3️⃣ 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests dnspython

      # 4️⃣ 执行脚本
      - name: Run blocklist update
        run: |
          python split_and_check_16.py

      # 5️⃣ 提交更新
      - name: Commit & push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/validated_*.txt tmp/part_*.txt
          if git diff-index --quiet HEAD; then
            echo "No changes to commit"
          else
            git commit -m "🤖 Auto update blocklist"
            git push
          fi
