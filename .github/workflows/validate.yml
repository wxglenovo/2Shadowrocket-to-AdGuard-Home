name: Update AdGuard Blocklist

on:
  schedule:
    - cron: "0 0 * * *"   # 每天 00:00 UTC
    - cron: "0 6 * * *"   # 每天 06:00 UTC
    - cron: "0 12 * * *"  # 每天 12:00 UTC
    - cron: "0 18 * * *"  # 每天 18:00 UTC
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180  # 3 小时，防止长时间 DNS 阻塞

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests dnspython

      - name: Run split & check script with live logging
        run: |
          echo "⏱ Workflow 开始：$(date -u)"
          python split_and_check_16.py | tee split_check.log
          echo "⏱ Workflow 结束：$(date -u)"

      - name: Commit and push updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/blocklist_valid.txt tmp/validated_*.txt
          git commit -m "🤖 Auto update: valid blocklist" || echo "No changes"
          git push origin main
