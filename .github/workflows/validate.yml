name: Validate Blocklist in 16 Parts

on:
  schedule:
    - cron: "0 */1 * * *"     # 每小时执行一次
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests dnspython

      - name: Run script
        run: python split_and_check_16.py

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/
          if git diff --cached --quiet; then
            echo "✅ 无变化，跳过提交"
            exit 0
          fi
          git commit -m "🤖 Auto update (part check $(date))"
          git pull --rebase origin main || true
          git push origin main || true
